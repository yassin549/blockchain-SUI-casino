// api/index.ts
// This file is the new entry point for Vercel's serverless environment.

import app from '../server/app';

// Vercel expects a default export that is a request handler
export default app;
